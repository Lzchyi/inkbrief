# Copy this file to base-url and replace the line with an HTTPS release root.
# A manual Dashboard launch checks this root; KUAL Update also uses it.
# Nothing runs at boot or on a background device schedule.
https://example.invalid/kindle-brief
